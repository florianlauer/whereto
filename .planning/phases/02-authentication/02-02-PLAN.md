---
phase: 02-authentication
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/stores/authModalStore.ts
  - src/hooks/useAuthGatedAction.ts
  - src/components/auth/__tests__/AuthModal.test.tsx
  - src/components/auth/AuthModal.tsx
  - src/components/auth/EmailPasswordForm.tsx
  - src/components/auth/MagicLinkForm.tsx
  - src/components/auth/OAuthButton.tsx
  - src/components/auth/UserMenu.tsx
  - src/main.tsx
autonomous: false
requirements: [AUTH-05, AUTH-06]

must_haves:
  truths:
    - "Auth modal appears only when anonymous user tries to save to wishlist, never on map load or filter change"
    - "'Continuer sans compte' button is always visible in the auth modal and dismisses it"
    - "User can switch between email/password, magic link, and Google sign-in within the modal"
    - "After successful sign-in, the modal closes automatically"
    - "Signed-in user sees their email/avatar and a sign-out button"
    - "Sign out returns app to anonymous mode"
  artifacts:
    - path: "src/stores/authModalStore.ts"
      provides: "Modal open/close state with pending action storage"
      exports: ["useAuthModalStore"]
    - path: "src/components/auth/AuthModal.tsx"
      provides: "Auth modal with tabbed sign-in methods"
      exports: ["AuthModal"]
    - path: "src/hooks/useAuthGatedAction.ts"
      provides: "Hook that gates actions behind auth, opening modal if anonymous"
      exports: ["useAuthGatedAction"]
    - path: "src/components/auth/UserMenu.tsx"
      provides: "Signed-in user display with sign-out action"
      exports: ["UserMenu"]
  key_links:
    - from: "src/hooks/useAuthGatedAction.ts"
      to: "src/stores/authModalStore.ts"
      via: "opens modal with pending action when user is anonymous"
      pattern: "useAuthModalStore.*open"
    - from: "src/hooks/useAuthGatedAction.ts"
      to: "src/stores/authStore.ts"
      via: "checks user state to determine if auth is needed"
      pattern: "useAuthStore.*user"
    - from: "src/components/auth/AuthModal.tsx"
      to: "src/stores/authStore.ts"
      via: "calls sign-in methods and reacts to user state"
      pattern: "useAuthStore"
    - from: "src/components/auth/AuthModal.tsx"
      to: "src/stores/authModalStore.ts"
      via: "reads isOpen, executes pendingAction on auth success, calls close"
      pattern: "useAuthModalStore"
---

<objective>
Auth UI: modal with three sign-in methods, auth-gated action hook, user menu with sign-out. The modal is triggered only on wishlist save actions (never gates the map), and always shows "Continuer sans compte".

Purpose: Deliver the user-facing auth experience that satisfies AUTH-05 (non-blocking UX) and AUTH-06 (continue without account).
Output: Complete auth UI components, auth-gated action hook, and component tests.
</objective>

<execution_context>
@/Users/florianlauer/.claude/get-shit-done/workflows/execute-plan.md
@/Users/florianlauer/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-authentication/02-RESEARCH.md
@.planning/phases/02-authentication/02-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 outputs (will exist when this plan runs) -->

From src/stores/authStore.ts (created in Plan 01):

```typescript
type AuthStore = {
  user: User | null;
  session: Session | null;
  loading: boolean;
  initialize: () => () => void;
  signUpWithEmail: (email: string, password: string) => Promise<{ data; error }>;
  signInWithEmail: (email: string, password: string) => Promise<{ data; error }>;
  signInWithMagicLink: (email: string) => Promise<{ error }>;
  signInWithGoogle: () => Promise<{ error }>;
  signOut: () => Promise<void>;
};
export const useAuthStore: UseBoundStore<StoreApi<AuthStore>>;
```

From src/lib/supabase.ts (created in Plan 01):

```typescript
export const supabase: SupabaseClient<Database>;
```

Existing UI patterns (from codebase):

- Tailwind CSS 4 for styling
- sonner for toast notifications (already in main.tsx)
- French UI language (all labels in French)
- Dark theme (bg-gray-950 text-white pattern)
  </interfaces>
  </context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create auth modal store, auth-gated action hook, and component tests</name>
  <files>src/stores/authModalStore.ts, src/hooks/useAuthGatedAction.ts, src/components/auth/__tests__/AuthModal.test.tsx</files>
  <behavior>
    - Test "modal opens on save action when anonymous": useAuthGatedAction wraps an action; when user is null, it opens the modal instead of executing
    - Test "modal does not open when authenticated": when user exists, action executes directly
    - Test "continuer sans compte visible": AuthModal renders a "Continuer sans compte" button that closes the modal
    - Test "modal closes on successful auth": when user state changes from null to a user, modal auto-closes and pending action executes
    - Test "three sign-in methods visible": modal shows email/password form, magic link option, and Google button
  </behavior>
  <action>
    1. Create `src/stores/authModalStore.ts`:
       - State: `isOpen: boolean`, `pendingAction: (() => void) | null`
       - Actions: `open(pendingAction?: () => void)` sets isOpen=true and stores pendingAction, `close()` sets isOpen=false and clears pendingAction, `executePending()` calls pendingAction if exists then closes.
       - Export `useAuthModalStore`.

    2. Create `src/hooks/useAuthGatedAction.ts`:
       - Reads `user` from `useAuthStore` and `open` from `useAuthModalStore`.
       - Returns a function `gateAction(action: () => void)`: if `user` exists, call `action()` directly. If not, call `open(action)` to open modal with pending action.
       - Export `useAuthGatedAction`.

    3. Create `src/components/auth/__tests__/AuthModal.test.tsx`:
       - Mock `src/stores/authStore` and `src/stores/authModalStore`.
       - Test AUTH-05: render AuthModal when isOpen=true and user=null, verify "Se connecter" title visible, verify email form visible, verify Google button visible.
       - Test AUTH-06: verify "Continuer sans compte" button is rendered and calls close() when clicked.
       - Test modal does not render when isOpen=false.
       - Test useAuthGatedAction: when user is null, calling gateAction opens modal; when user exists, action fires directly.

  </action>
  <verify>
    <automated>cd /Users/florianlauer/Documents/perso/whereto && bunx vitest run src/components/auth/__tests__/AuthModal.test.tsx</automated>
  </verify>
  <done>Auth modal store manages open/close/pending state. useAuthGatedAction gates actions behind auth. Component tests pass for modal visibility, "Continuer sans compte", and auth gating behavior.</done>
</task>

<task type="auto">
  <name>Task 2: Create auth UI components and wire AuthModal into the app</name>
  <files>src/components/auth/EmailPasswordForm.tsx, src/components/auth/MagicLinkForm.tsx, src/components/auth/OAuthButton.tsx, src/components/auth/AuthModal.tsx, src/components/auth/UserMenu.tsx, src/main.tsx</files>
  <action>
    1. Create `src/components/auth/EmailPasswordForm.tsx`:
       - Two modes: "sign-in" and "sign-up" with a toggle link at bottom ("Pas encore de compte ? Creer un compte" / "Deja un compte ? Se connecter").
       - Fields: email (type="email", required), password (type="password", required, minLength=6).
       - On submit: calls `useAuthStore` signUpWithEmail or signInWithEmail depending on mode.
       - Shows error from Supabase via `toast.error()`. Shows success for signup via `toast.success("Compte cree !")`.
       - Submit button text: "Se connecter" / "Creer un compte". Disabled while loading.
       - All labels in French. Use Tailwind for styling, dark theme inputs (bg-gray-800 border-gray-700 text-white).

    2. Create `src/components/auth/MagicLinkForm.tsx`:
       - Single email field.
       - On submit: calls `useAuthStore.signInWithMagicLink(email)`.
       - Success: `toast.success("Lien magique envoye ! Verifiez votre boite mail.")`.
       - Error: `toast.error(error.message)`.
       - Submit button: "Envoyer le lien magique". Disabled while loading.

    3. Create `src/components/auth/OAuthButton.tsx`:
       - Button with Google icon (inline SVG or text "Google").
       - On click: calls `useAuthStore.signInWithGoogle()`.
       - Error: `toast.error(error.message)`.
       - Button text: "Continuer avec Google". Full width, distinct style (bg-white text-gray-900 for contrast).

    4. Create `src/components/auth/AuthModal.tsx`:
       - Reads `isOpen` and `close` from `useAuthModalStore`, `user` from `useAuthStore`.
       - Renders a modal overlay (fixed inset-0 bg-black/50 z-50) with centered content panel (bg-gray-900 rounded-xl p-6 max-w-md w-full mx-4).
       - Content: Title "Se connecter", then OAuthButton (Google first -- most prominent), horizontal divider "ou", then tab-like toggle between "Email / Mot de passe" and "Lien magique" showing the appropriate form.
       - "Continuer sans compte" button at bottom: calls `close()`, styled as text/link button (text-gray-400 underline).
       - Auto-close: `useEffect` watches `user` -- when it changes from null to a user while modal is open, call `executePending()` from authModalStore (which runs the pending action and closes).
       - Click outside (on overlay) calls `close()`.
       - Escape key closes modal.

    5. Create `src/components/auth/UserMenu.tsx`:
       - Reads `user` from `useAuthStore`.
       - If no user: renders nothing (or a "Se connecter" button that opens the auth modal -- use `useAuthModalStore.open()`).
       - If user: shows user email (truncated if long), and a "Deconnexion" button that calls `useAuthStore.signOut()`.
       - Compact design suitable for a header/navbar position.

    6. Wire AuthModal into the app: Add `<AuthModal />` to `src/main.tsx` inside the App component, after `<Toaster />`. This ensures the modal is globally available.

  </action>
  <verify>
    <automated>cd /Users/florianlauer/Documents/perso/whereto && bunx vitest run src/components/auth/__tests__/AuthModal.test.tsx && bun run type 2>&1 | head -20</automated>
  </verify>
  <done>All auth UI components render correctly. AuthModal shows 3 sign-in methods with "Continuer sans compte". UserMenu displays user info and sign-out. AuthModal wired into main.tsx. All tests pass. TypeScript clean.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify auth flow visually</name>
  <files>none</files>
  <action>
    What was built: Complete auth UI -- modal with email/password, magic link, Google OAuth, "Continuer sans compte" dismiss, and UserMenu with sign-out.

    How to verify:
    1. Start dev server: `bun run dev`
    2. Verify the map loads WITHOUT any auth modal or login prompt (AUTH-05)
    3. Open browser console, run: `window.__authModalStore = (await import('/src/stores/authModalStore.ts')).useAuthModalStore; window.__authModalStore.getState().open()` to trigger the modal
    4. Verify auth modal appears with:
       - "Se connecter" title
       - "Continuer avec Google" button
       - Email/password form with "Se connecter" / "Creer un compte" toggle
       - "Lien magique" tab option
       - "Continuer sans compte" link at bottom (AUTH-06)
    5. Click "Continuer sans compte" -- modal should close
    6. Re-open modal, try signing up with email/password (use a test email if Supabase local is running, or just verify the form submits)
    7. If you have Google OAuth configured in Supabase: click "Continuer avec Google" and verify redirect preserves URL params (AUTH-07)
    8. If signed in: verify UserMenu shows email and "Deconnexion" button
    9. Click "Deconnexion" -- verify app returns to anonymous mode

    Resume signal: Type "approved" or describe issues.

  </action>
  <verify>Human visual inspection</verify>
  <done>Auth modal displays correctly with all 3 methods, "Continuer sans compte" works, sign-in/sign-out flow functional. Human approved.</done>
</task>

</tasks>

<verification>
1. `bunx vitest run src/components/auth/__tests__/AuthModal.test.tsx` -- all UI tests pass
2. `bunx vitest run src/stores/__tests__/authStore.test.ts` -- auth store tests still pass (regression)
3. `bun run type` -- no TypeScript errors
4. Visual: modal appears only on trigger, not on page load
5. Visual: "Continuer sans compte" is always visible in modal
6. Visual: all three auth methods accessible in modal
</verification>

<success_criteria>

- Auth modal with email/password, magic link, and Google OAuth
- Modal triggered only by auth-gated save actions, never on map load
- "Continuer sans compte" button always visible and functional
- Modal auto-closes on successful authentication
- Pending action executes after auth success
- UserMenu shows user info and sign-out option
- Sign-out returns to anonymous mode
- All component tests pass
- Human verification approved
  </success_criteria>

<output>
After completion, create `.planning/phases/02-authentication/02-02-SUMMARY.md`
</output>
