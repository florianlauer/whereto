---
phase: 02-authentication
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/supabase.ts
  - src/stores/authStore.ts
  - src/server/trpc.ts
  - src/server/middleware.ts
  - src/main.tsx
  - src/lib/__mocks__/supabase.ts
  - src/stores/__tests__/authStore.test.ts
autonomous: true
requirements: [AUTH-01, AUTH-02, AUTH-03, AUTH-04, AUTH-07]

must_haves:
  truths:
    - "Supabase auth methods (signUp, signInWithPassword, signInWithOtp, signInWithOAuth, signOut) are callable from client"
    - "Auth state (user, session, loading) is tracked in Zustand and reactive"
    - "onAuthStateChange listener updates store synchronously (no async in callback)"
    - "tRPC protected procedures reject requests without valid Bearer token"
    - "tRPC client sends Authorization header when session exists"
    - "OAuth redirectTo preserves current URL with search params"
  artifacts:
    - path: "src/lib/supabase.ts"
      provides: "Client-side Supabase instance using VITE_ env vars"
      exports: ["supabase"]
    - path: "src/stores/authStore.ts"
      provides: "Auth Zustand store with user, session, loading, actions"
      exports: ["useAuthStore"]
    - path: "src/server/middleware.ts"
      provides: "tRPC auth middleware and protectedProcedure"
      exports: ["protectedProcedure"]
    - path: "src/server/trpc.ts"
      provides: "Updated createContext with accessToken extraction from headers"
      exports: ["createContext", "Context", "router", "publicProcedure"]
  key_links:
    - from: "src/stores/authStore.ts"
      to: "src/lib/supabase.ts"
      via: "import supabase client for auth calls"
      pattern: "import.*supabase.*from.*lib/supabase"
    - from: "src/main.tsx"
      to: "src/stores/authStore.ts"
      via: "initialize auth listener on mount, inject token into tRPC headers"
      pattern: "useAuthStore.*initialize|getState.*session"
    - from: "src/server/middleware.ts"
      to: "src/server/db.ts"
      via: "createSupabaseClient with user token for RLS-scoped queries"
      pattern: "createSupabaseClient.*accessToken"
    - from: "src/server/trpc.ts"
      to: "api/index.ts"
      via: "createContext receives request from Hono adapter"
      pattern: "createContext"
---

<objective>
Auth core infrastructure: client Supabase instance, Zustand auth store with all three sign-in methods, tRPC auth context with protected procedure middleware, and auth header injection in the tRPC client.

Purpose: Establish the complete auth plumbing so that Plan 02 (UI) can call auth actions and protected API routes work end-to-end.
Output: Working auth store, server middleware, and test scaffolds for all auth requirements.
</objective>

<execution_context>
@/Users/florianlauer/.claude/get-shit-done/workflows/execute-plan.md
@/Users/florianlauer/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-authentication/02-RESEARCH.md
@.planning/phases/02-authentication/02-VALIDATION.md
@.planning/phases/01-backend-foundation/01-02-SUMMARY.md

<interfaces>
<!-- Existing interfaces the executor needs -->

From src/server/trpc.ts:

```typescript
export function createContext() {
  return { supabaseAdmin };
}
export type Context = Awaited<ReturnType<typeof createContext>>;
export const router = t.router;
export const publicProcedure = t.procedure;
```

From src/server/db.ts:

```typescript
export const supabaseAdmin = createClient<Database>(...);
export function createSupabaseClient(supabaseAccessToken?: string);
```

From src/lib/trpc.ts:

```typescript
export { TRPCProvider, useTRPC, useTRPCClient };
```

From src/server/router.ts:

```typescript
export const appRouter = router({ health, wishlist, profile });
export type AppRouter = typeof appRouter;
```

From api/index.ts:

```typescript
// Hono app with trpcServer middleware calling createContext
// IMPORTANT: @hono/trpc-server passes opts differently -- check req.header() vs req.headers.get()
```

From src/main.tsx:

```typescript
// trpcClient created with httpBatchLink({ url: "/api/trpc" })
// App component wraps with QueryClientProvider > TRPCProvider > RouterProvider
```

</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create client Supabase instance, auth store, and Wave 0 test scaffolds</name>
  <files>src/lib/supabase.ts, src/stores/authStore.ts, src/lib/__mocks__/supabase.ts, src/stores/__tests__/authStore.test.ts</files>
  <behavior>
    - Test "email signup": signUp called with email+password, store updates user on success
    - Test "email signin": signInWithPassword called correctly
    - Test "magic link": signInWithOtp called with email and emailRedirectTo containing current URL
    - Test "google oauth": signInWithOAuth called with provider 'google' and redirectTo containing current URL (AUTH-07)
    - Test "session persistence": getSession returns cached session, store initializes with it (AUTH-04)
    - Test "redirect preserves URL": redirectTo option includes window.location.href (AUTH-07)
    - Test "signout": signOut clears user and session to null
    - Test "onAuthStateChange is sync": callback only calls set(), no async/await
  </behavior>
  <action>
    1. Create `src/lib/supabase.ts`: Single client-side Supabase instance using `import.meta.env.VITE_SUPABASE_URL` and `import.meta.env.VITE_SUPABASE_ANON_KEY`. Export as `supabase`. Type with `Database` from `../lib/database.types`.

    2. Create `src/lib/__mocks__/supabase.ts`: Mock that exports a `supabase` object with `auth.signUp`, `auth.signInWithPassword`, `auth.signInWithOtp`, `auth.signInWithOAuth`, `auth.signOut`, `auth.getSession`, `auth.getUser`, `auth.onAuthStateChange` as vi.fn() mocks. `onAuthStateChange` returns `{ data: { subscription: { unsubscribe: vi.fn() } } }`.

    3. Create `src/stores/__tests__/authStore.test.ts`: Tests covering AUTH-01 (email signup/signin), AUTH-02 (magic link), AUTH-03 (google oauth), AUTH-04 (session persistence), AUTH-07 (redirect URL preservation). Mock `src/lib/supabase` using vi.mock. Each test calls the store action and asserts the correct Supabase auth method was called with correct params.

    4. Create `src/stores/authStore.ts`: Zustand store with:
       - State: `user: User | null`, `session: Session | null`, `loading: boolean`
       - `initialize()`: Calls `supabase.auth.getSession()` then sets state. Sets up `onAuthStateChange` listener that ONLY calls `set()` synchronously (NO async/await in callback per deadlock pitfall). Returns unsubscribe function.
       - `signUpWithEmail(email, password)`: Calls `supabase.auth.signUp({ email, password })`, returns `{ data, error }`.
       - `signInWithEmail(email, password)`: Calls `supabase.auth.signInWithPassword({ email, password })`, returns `{ data, error }`.
       - `signInWithMagicLink(email)`: Calls `supabase.auth.signInWithOtp({ email, options: { emailRedirectTo: window.location.href } })`, returns `{ error }`.
       - `signInWithGoogle()`: Calls `supabase.auth.signInWithOAuth({ provider: 'google', options: { redirectTo: window.location.href } })`, returns `{ error }`.
       - `signOut()`: Calls `supabase.auth.signOut()`. The `onAuthStateChange` callback handles clearing state.

    Import types from `@supabase/supabase-js` (User, Session). Use `@/lib/supabase` import for the client.

  </action>
  <verify>
    <automated>cd /Users/florianlauer/Documents/perso/whereto && bunx vitest run src/stores/__tests__/authStore.test.ts</automated>
  </verify>
  <done>Auth store has all 3 sign-in methods + sign out + initialize. Tests pass for email, magic link, google, session, redirect, signout behaviors. Mock supabase helper available for future tests.</done>
</task>

<task type="auto">
  <name>Task 2: Add tRPC auth context, protected procedure middleware, and wire auth headers into client</name>
  <files>src/server/trpc.ts, src/server/middleware.ts, src/main.tsx</files>
  <action>
    1. Update `src/server/trpc.ts`:
       - Change `createContext()` to accept request info from Hono adapter. The `@hono/trpc-server` adapter passes `(opts)` where opts contains the Hono context. Check the adapter source: it calls `createContext({ req: c.req.raw })` where `c.req.raw` is a standard `Request` object. So the signature should be `createContext(opts?: { req: Request })`.
       - Extract `Authorization` header: `const authHeader = opts?.req?.headers.get('authorization')`. Extract token: `const accessToken = authHeader?.startsWith('Bearer ') ? authHeader.slice(7) : undefined`.
       - Return `{ supabaseAdmin, supabaseUser: createSupabaseClient(accessToken), accessToken }`.
       - Update the `Context` type export accordingly.
       - Keep exporting `router` and `publicProcedure`.

    2. Create `src/server/middleware.ts`:
       - Import `t` ... actually, the `t` instance is private to trpc.ts. Instead, export the middleware builder from trpc.ts. Update `src/server/trpc.ts` to also export `const middleware = t.middleware` and `const baseProcedure = t.procedure`.
       - In `src/server/middleware.ts`: Create `isAuthed` middleware that checks `ctx.accessToken` (throw UNAUTHORIZED if missing), then calls `ctx.supabaseUser.auth.getUser()` (NOT getSession -- security pitfall), throws UNAUTHORIZED if error or no user, then calls `next({ ctx: { ...ctx, user } })`.
       - Export `protectedProcedure = baseProcedure.use(isAuthed)`.

    3. Update `src/main.tsx`:
       - Add auth initialization: Inside `App` component, add `useEffect` that calls `useAuthStore.getState().initialize()` and returns the unsubscribe cleanup.
       - Update `trpcClient` creation: In `httpBatchLink`, add `headers` callback that reads `useAuthStore.getState().session?.access_token` and returns `{ authorization: \`Bearer \${token}\` }` if token exists, empty object otherwise. NOTE: Use `getState()` (not hook) since this is outside React render.

    4. Update `src/server/routers/profile.ts` to use `protectedProcedure` for the `me` query:
       - Import `protectedProcedure` from `../middleware`.
       - Change `me` to use `protectedProcedure.query(({ ctx }) => { return { id: ctx.user.id, email: ctx.user.email }; })`.
       This validates the auth middleware works end-to-end.

  </action>
  <verify>
    <automated>cd /Users/florianlauer/Documents/perso/whereto && bun run type 2>&1 | head -20</automated>
  </verify>
  <done>tRPC context extracts Bearer token from Authorization header. protectedProcedure rejects unauthenticated requests (UNAUTHORIZED). tRPC client injects session token in headers. Auth store initializes on app mount. profile.me uses protectedProcedure. TypeScript compiles cleanly.</done>
</task>

</tasks>

<verification>
1. `bunx vitest run src/stores/__tests__/authStore.test.ts` -- all auth store tests pass
2. `bun run type` -- no TypeScript errors across client and server
3. Auth store exports: useAuthStore with user, session, loading, initialize, signUpWithEmail, signInWithEmail, signInWithMagicLink, signInWithGoogle, signOut
4. protectedProcedure is importable and usable in routers
5. tRPC client sends Authorization header when session exists
</verification>

<success_criteria>

- Client Supabase instance created at src/lib/supabase.ts
- Auth store with all 3 sign-in methods, sign out, and session initialization
- onAuthStateChange callback is synchronous (no async/await in callback body)
- tRPC createContext extracts Bearer token from request headers
- protectedProcedure middleware validates user via getUser() (not getSession)
- tRPC client injects access_token in Authorization header
- Auth store initializes on app mount via useEffect
- All auth store unit tests pass
- TypeScript compiles without errors
  </success_criteria>

<output>
After completion, create `.planning/phases/02-authentication/02-01-SUMMARY.md`
</output>
