---
phase: 01-backend-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - package.json
  - api/index.ts
  - src/server/trpc.ts
  - src/server/db.ts
  - src/server/router.ts
  - src/server/routers/health.ts
  - src/server/routers/wishlist.ts
  - src/server/routers/profile.ts
  - src/lib/trpc.ts
  - src/main.tsx
  - tsconfig.server.json
  - tsconfig.json
  - .gitignore
  - .env.example
autonomous: true
requirements:
  - INFRA-02
  - INFRA-04

must_haves:
  truths:
    - "A tRPC call to /api/trpc/health.ping returns a valid JSON response (not SPA HTML)"
    - "The SUPABASE_SERVICE_ROLE_KEY is not present in the Vite client bundle (dist/)"
    - "The React app renders with TRPCProvider and QueryClientProvider without errors"
    - "Server code is type-checked alongside client code"
  artifacts:
    - path: "api/index.ts"
      provides: "Vercel serverless entry point with Hono + tRPC"
      exports: ["default"]
      contains: "trpcServer"
    - path: "src/server/trpc.ts"
      provides: "tRPC init, context creation, procedure exports"
      exports: ["router", "publicProcedure", "createContext"]
    - path: "src/server/db.ts"
      provides: "Supabase server-side client factory"
      exports: ["supabaseAdmin"]
      contains: "SUPABASE_SERVICE_ROLE_KEY"
    - path: "src/server/router.ts"
      provides: "App router merging domain routers"
      exports: ["appRouter", "AppRouter"]
    - path: "src/lib/trpc.ts"
      provides: "Client-side tRPC context (TRPCProvider, useTRPC)"
      exports: ["TRPCProvider", "useTRPC"]
    - path: "src/main.tsx"
      provides: "App entry with tRPC + React Query providers"
      contains: "TRPCProvider"
    - path: "tsconfig.server.json"
      provides: "TypeScript config covering api/ and src/server/"
      contains: "api"
    - path: ".env.example"
      provides: "Template for required env vars"
      contains: "SUPABASE_SERVICE_ROLE_KEY"
  key_links:
    - from: "api/index.ts"
      to: "src/server/router.ts"
      via: "import appRouter"
      pattern: "import.*appRouter.*from"
    - from: "src/server/trpc.ts"
      to: "src/server/db.ts"
      via: "import supabaseAdmin"
      pattern: "import.*supabaseAdmin"
    - from: "src/lib/trpc.ts"
      to: "src/server/router.ts"
      via: "import type AppRouter"
      pattern: "import type.*AppRouter"
    - from: "src/main.tsx"
      to: "src/lib/trpc.ts"
      via: "import TRPCProvider"
      pattern: "import.*TRPCProvider"
---

<objective>
Install tRPC + Hono dependencies, create the server-side API (Hono entry point + tRPC routers), wire the tRPC client into the React app, configure TypeScript for server code, and enforce env var security.

Purpose: Make `/api/trpc/*` respond with valid tRPC responses on Vercel, and give the React app a typed tRPC client ready for Phase 2+ to add authenticated procedures.
Output: Working API endpoint, tRPC client in React, server tsconfig, env var security verified.
</objective>

<execution_context>
@/Users/florianlauer/.claude/get-shit-done/workflows/execute-plan.md
@/Users/florianlauer/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-backend-foundation/01-CONTEXT.md
@.planning/phases/01-backend-foundation/01-RESEARCH.md
@.planning/phases/01-backend-foundation/01-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 output: generated types file -->
From src/lib/database.types.ts:
```typescript
export type Database = {
  public: {
    Tables: {
      profiles: { Row: {...}, Insert: {...}, Update: {...} }
      wishlists: { Row: {...}, Insert: {...}, Update: {...} }
      wishlist_items: { Row: {...}, Insert: {...}, Update: {...} }
    }
  }
}
```

<!-- Existing entry point constraint -->

From vercel.json:

```json
{ "source": "/api/(.*)", "destination": "/api/index.ts" }
```

The Vercel rewrite is locked -- server entry must be at `api/index.ts`.

<!-- Existing main.tsx structure that needs TRPCProvider wrapping -->

From src/main.tsx:

```typescript
// Current render tree:
// <StrictMode> -> <ErrorBoundary> -> <App /> (loads data, then <RouterProvider> + <Toaster>)
// TRPCProvider + QueryClientProvider must wrap inside App, around RouterProvider
```

<!-- Existing tsconfig structure -->

From tsconfig.json:

```json
{
  "references": [{ "path": "./tsconfig.app.json" }, { "path": "./tsconfig.node.json" }]
}
```

A new `tsconfig.server.json` reference should be added here.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install dependencies and create tRPC server with Hono entry point</name>
  <files>package.json, api/index.ts, src/server/trpc.ts, src/server/db.ts, src/server/router.ts, src/server/routers/health.ts, src/server/routers/wishlist.ts, src/server/routers/profile.ts, tsconfig.server.json, tsconfig.json, .env.example, .gitignore</files>
  <action>
1. Install server dependencies:
   ```bash
   bun add @trpc/server @trpc/client @trpc/tanstack-react-query @tanstack/react-query hono @hono/trpc-server @hono/node-server @supabase/supabase-js
   bun add -d @tanstack/react-query-devtools
   ```
   Pin `@hono/trpc-server` >= 0.4.2 and `@trpc/server` >= 11.10 to avoid the known version mismatch pitfall.

2. Create `src/server/db.ts` -- Supabase server-side client factory:
   - Export `supabaseAdmin` using `createClient<Database>` with `process.env.SUPABASE_URL` and `process.env.SUPABASE_SERVICE_ROLE_KEY` (NO `VITE_` prefix)
   - Import `Database` type from `../lib/database.types` (generated in Plan 01)
   - Export `createSupabaseClient(supabaseAccessToken?: string)` for future per-user client (Phase 2)

3. Create `src/server/trpc.ts` -- tRPC initialization:
   - Import `initTRPC` from `@trpc/server`
   - Import `supabaseAdmin` from `./db`
   - `createContext()` returns `{ supabaseAdmin }` (expanded in Phase 2 with user JWT)
   - Export `router`, `publicProcedure`, `createContext`

4. Create `src/server/routers/health.ts`:
   - `healthRouter` with a `ping` query returning `{ status: "ok", timestamp: new Date().toISOString() }`

5. Create `src/server/routers/wishlist.ts`:
   - Stub router with a placeholder `list` query returning empty array (Phase 3 fills this in)

6. Create `src/server/routers/profile.ts`:
   - Stub router with a placeholder `me` query returning null (Phase 2 fills this in)

7. Create `src/server/router.ts` -- App router:
   - Merge `healthRouter`, `wishlistRouter`, `profileRouter`
   - Export `appRouter` and `type AppRouter = typeof appRouter`

8. Create `api/index.ts` -- Hono entry point:
   - Create Hono app with `.basePath("/api")`
   - Add `GET /health` endpoint returning `{ ok: true }`
   - Mount tRPC on `/trpc/*` using `@hono/trpc-server` middleware with `appRouter` and `createContext`
   - Export default using `handle()` from `@hono/node-server/vercel` (Node.js runtime, not Edge)

9. Create `tsconfig.server.json`:
   - Extends: nothing (standalone)
   - `compilerOptions`: target ES2022, module ESNext, moduleResolution bundler, strict true, noEmit true, skipLibCheck true, paths `@/*` -> `./src/*`, esModuleInterop true, baseUrl "."
   - `include`: ["api", "src/server", "src/lib/database.types.ts"]

10. Update `tsconfig.json` to add `{ "path": "./tsconfig.server.json" }` to the `references` array.

11. Create `.env.example` with all required env vars (values empty, comments explaining source):

    ```
    # Server-only (Vercel env vars, NOT prefixed with VITE_)
    SUPABASE_URL=
    SUPABASE_ANON_KEY=
    SUPABASE_SERVICE_ROLE_KEY=

    # Client-safe (exposed in browser bundle)
    VITE_SUPABASE_URL=
    VITE_SUPABASE_ANON_KEY=
    ```

12. Ensure `.gitignore` includes `.env` and `.env.local` (do NOT commit secrets). Also add `supabase/.temp/` if not already there.
    </action>
    <verify>
    <automated>cd /Users/florianlauer/Documents/perso/whereto && npx tsc --project tsconfig.server.json --noEmit 2>&1 | head -20</automated>
    </verify>
    <done>All server files compile without TypeScript errors. api/index.ts exports Hono app with tRPC mounted. Server code uses process.env (not VITE\_) for service_role key. tsconfig.server.json covers api/ and src/server/.</done>
    </task>

<task type="auto">
  <name>Task 2: Wire tRPC client into React app and verify security</name>
  <files>src/lib/trpc.ts, src/main.tsx</files>
  <action>
1. Create `src/lib/trpc.ts` -- Client-side tRPC setup:
   - Use tRPC v11 pattern: `createTRPCContext<AppRouter>()` from `@trpc/tanstack-react-query`
   - Export `TRPCProvider`, `useTRPC`, `useTRPCClient`
   - Import `AppRouter` using `import type` ONLY (critical: prevents server code leaking into client bundle)

2. Update `src/main.tsx`:
   - Import `QueryClient`, `QueryClientProvider` from `@tanstack/react-query`
   - Import `createTRPCClient`, `httpBatchLink` from `@trpc/client`
   - Import `TRPCProvider` from `@/lib/trpc`
   - Create `queryClient` at module level with `defaultOptions: { queries: { staleTime: 30_000 } }`
   - Create `trpcClient` at module level with `httpBatchLink({ url: "/api/trpc" })`
   - Inside the `App` component, wrap `<RouterProvider>` with `<QueryClientProvider client={queryClient}>` then `<TRPCProvider trpcClient={trpcClient} queryClient={queryClient}>`
   - Keep existing loading state, error boundary, and Toaster unchanged
   - Optionally add `<ReactQueryDevtools />` in dev mode only

3. Run `bun run build` to produce `dist/` and verify INFRA-04:
   - `grep -r "service_role" dist/` must return NO matches
   - `grep -r "SUPABASE_SERVICE_ROLE_KEY" dist/` must return NO matches

4. Run `bun run type` to verify full type-check passes (both client and server configs).
   </action>
   <verify>
   <automated>cd /Users/florianlauer/Documents/perso/whereto && bun run build 2>&1 | tail -3 && ! grep -r "service_role" dist/ && echo "INFRA-04: PASS - no service_role in bundle"</automated>
   </verify>
   <done>React app renders with TRPCProvider wrapping the router. Build succeeds. No service_role key appears in dist/ bundle. `bun run type` passes for both client and server tsconfigs.</done>
   </task>

</tasks>

<verification>
1. `bun run build` succeeds without errors
2. `grep -r "service_role" dist/` returns no matches (INFRA-04)
3. `npx tsc --project tsconfig.server.json --noEmit` passes (server types valid)
4. `bun run type` passes (client types valid)
5. `bun run test` passes (existing tests still work)
6. Local dev: `bun run dev` starts without errors, app renders normally
7. API verification (when deployed or via local Vercel dev): `curl /api/trpc/health.ping` returns `{"result":{"data":{"status":"ok",...}}}`
</verification>

<success_criteria>

- tRPC health.ping endpoint returns valid JSON at /api/trpc/health.ping (INFRA-02)
- SUPABASE_SERVICE_ROLE_KEY absent from client bundle dist/ (INFRA-04)
- React app wraps with TRPCProvider + QueryClientProvider, renders without errors
- Server code type-checks via tsconfig.server.json
- All existing tests continue to pass
  </success_criteria>

<output>
After completion, create `.planning/phases/01-backend-foundation/01-02-SUMMARY.md`
</output>
