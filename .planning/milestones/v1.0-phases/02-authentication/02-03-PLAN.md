---
phase: 02-authentication
plan: 03
type: execute
wave: 3
depends_on: ["02-02"]
files_modified:
  - src/components/filters/FilterBar.tsx
autonomous: true
requirements: [AUTH-01, AUTH-02, AUTH-03, AUTH-04, AUTH-05, AUTH-06, AUTH-07]
gap_closure: true

must_haves:
  truths:
    - "Signed-in user sees their email and a Deconnexion button in the top bar"
    - "Anonymous user sees a Se connecter button in the top bar"
    - "Clicking Deconnexion signs the user out and the button reverts to Se connecter"
  artifacts:
    - path: "src/components/filters/FilterBar.tsx"
      provides: "UserMenu rendered in filter bar header"
      contains: "UserMenu"
  key_links:
    - from: "src/components/filters/FilterBar.tsx"
      to: "src/components/auth/UserMenu.tsx"
      via: "import and render"
      pattern: "import.*UserMenu.*from.*auth/UserMenu"
---

<objective>
Wire the orphaned UserMenu component into the app's visible UI.

Purpose: Close the single verification gap -- UserMenu.tsx exists with correct implementation but is not rendered anywhere. Success Criterion 5 ("User can sign out from the UI") cannot be satisfied without this fix.

Output: UserMenu visible in the FilterBar header, right-aligned after the filter buttons.
</objective>

<execution_context>
@/Users/florianlauer/.claude/get-shit-done/workflows/execute-plan.md
@/Users/florianlauer/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-authentication/02-VERIFICATION.md

<interfaces>
<!-- UserMenu component to wire in -->

From src/components/auth/UserMenu.tsx:

```typescript
export function UserMenu();
// When user is null: renders "Se connecter" button that opens auth modal
// When user exists: renders truncated email + "Deconnexion" button
```

From src/components/filters/FilterBar.tsx (target file):

```typescript
export function FilterBar({ search }: Props);
// Line 161: <div className="flex items-center gap-2 px-3 py-2.5 ...">
//   - Logo (hidden on mobile)
//   - Filter buttons (flex-1 overflow container)
//   - "Tout effacer" button (conditional, shrink-0)
//   ** UserMenu should go here, after "Tout effacer", as the last item **
// Line 335: </div>
```

</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire UserMenu into FilterBar</name>
  <files>src/components/filters/FilterBar.tsx</files>
  <action>
    1. Add import at top of FilterBar.tsx: `import { UserMenu } from '@/components/auth/UserMenu'`
    2. Insert `<UserMenu />` as the last child inside the header flex row (line 161's div), after the conditional "Tout effacer" button (after line 334's closing `)}`)
    3. The UserMenu is already self-contained (handles both anonymous and authenticated states). No props needed.

    Layout note: UserMenu will sit right-aligned in the top bar thanks to the existing flex layout. The filter buttons container has `flex-1` so it takes available space, pushing UserMenu to the right alongside "Tout effacer".

  </action>
  <verify>
    <automated>cd /Users/florianlauer/Documents/perso/whereto && bun run build && bun run test && grep -q "UserMenu" src/components/filters/FilterBar.tsx</automated>
  </verify>
  <done>UserMenu is imported and rendered in FilterBar. Build passes. Tests pass. grep confirms UserMenu is present in FilterBar.tsx. The component is no longer orphaned.</done>
</task>

</tasks>

<verification>
1. `grep -r "UserMenu" src/` returns hits in both UserMenu.tsx AND FilterBar.tsx (no longer orphaned)
2. `bun run build` succeeds (no type errors from new import)
3. `bun run test` all existing tests pass (no regressions)
</verification>

<success_criteria>

- UserMenu component rendered in the FilterBar header row
- Anonymous users see "Se connecter" button
- Authenticated users see their email + "Deconnexion" button
- All existing tests continue to pass
- Build succeeds
  </success_criteria>

<output>
After completion, create `.planning/phases/02-authentication/02-03-SUMMARY.md`
</output>
